# O registry corporativo deve estar configurado pelo usuário/organização.
# Não reescrever URLs que já apontam para um caminho próprio no Nexus.
# Não incluir credenciais neste arquivo.
save-exact=true
engine-strict=true
strict-peer-deps=true
package-lock=true
replace-registry-host=never
fund=false
audit=false
